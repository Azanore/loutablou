// Purpose: Vitest global test setup — extends expect with jest-dom matchers.
// Related: vite.config.ts (setupFiles)
// Must not include: component imports, test utilities

import '@testing-library/jest-dom'
